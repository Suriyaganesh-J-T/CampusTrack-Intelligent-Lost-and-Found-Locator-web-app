spring.application.name=campus-backend

# --- Database Configuration ---
spring.datasource.url=jdbc:mysql://localhost:3306/campus_portal?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Gokul@1051
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.jpa.properties.hibernate.hbm2ddl.auto=update


spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- JWT Configuration ---
jwt.secret=8b1f9c2d7a3e4f1d9b2e5c6d8f3a7b1c8d1e9f7c4a2d6b1e3f7c9d5a2b8e1f3
jwt.expiration-ms=86400000

# --- Server ---
server.port=8080

# --- File Upload ---
app.upload.dir=./uploads

# --- Campus domain verification (simple & quick) ---
# You can later change to @kingston.ac.in etc. if needed.
app.allowed.email.domains=@gmail.com
